# 📷 QR & Barcode Detection Project

This project is a Python-based **QR code and Barcode detection system** using **OpenCV** and **pyzbar**.  
It can scan codes from **image files** and also from a **live camera**.

## ✨ Features

- ✅ Detects **QR codes** and **Barcodes**
- 🖼️ Scans from image files (`.png`, `.jpg`)
- 🎥 Live camera scanning
- 🔊 Beep sound **only once per unique code**
- 📝 Displays scanned data in the terminal
- 🟩 Draws bounding boxes around detected codes
- 📍 Automatically adjusts text position inside the image

## 🛠️ Requirements

Make sure Python is installed, then install the required libraries:

```bash
pip install opencv-python pyzbar
⚠️ Windows users
winsound is built-in (no installation needed).

````
▶️ How to Run the Project
1️⃣ Open terminal in the project folder
cd qr_project

2️⃣ Run the program
python qr_test.py


🚀 Technologies Used

Python

OpenCV

Pyzbar

VS Code
